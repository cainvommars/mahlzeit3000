<img src="{{viewer.image}}" class="viewerImg"/>
<p>{{event.owner.name}} (<a target="_blank" href="http://twitter.com/{{event.owner.screen_name}}">@{{event.owner.screen_name}}</a>) invites to {{event.title}} at {{event.time}}</p>
<p>Are you in?</p>
<h1>{{event.title}}</h1>
{{#unless viewer.join}}
<div class="ready row">
  <button class="join_decline content" data-action="decline" data-event-id="{{event.id}}"
          data-viewer-id="{{viewer.hash}}">Decline
  </button>
  <button class="join_decline content" data-action="join" data-event-id="{{event.id}}" data-viewer-id="{{viewer.hash}}">
    Join
  </button>
</div>
{{/unless}}

Also invited:
<ul class="user-list">
  {{#each event.users}}
  <li class="{{status}} {{isViewer}}">
    <img src="{{image}}"/>

    <p><span>{{name}}</span>@{{screen_name}}</p>
  </li>
  {{/each}}
</ul>
<script src="/js/join.js"></script>
